# Submissions

Each atom goes in its own folder here, named by its id and then its version:

```text
submissions/your-atom-id/1.0.0/
```

Put your atom's files in that folder, including a `manifest.json`. See the
[repository README](../README.md) for the full steps on how to submit.
